/* import { testeArray } from "../database/data.js";
import { TesteType } from "../protocols/Types.js";

function insertObj(obj : TesteType){
    testeArray.push(obj);
    if (obj.id === undefined){
        return true
     }
     return false;
}

export {insertObj} */ 
